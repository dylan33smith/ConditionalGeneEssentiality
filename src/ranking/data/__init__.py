"""src.ranking.data — the ranking data layer.

Split (condition-holdout), eligibility, condition chemistry features, fingerprint
bundles, and the RankingBatch sampler contract for the top-k objective.
"""
