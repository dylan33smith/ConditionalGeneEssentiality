"""Verify that scaler artifacts are never derived from val/test data.

Stub test — implement once fit_condition_scalers.py is complete.
"""
import pytest


@pytest.mark.skip(reason="Stub — implement after fit_condition_scalers.py")
def test_scaler_fit_excludes_val():
    """Scaler fitted only on train amounts must not see val amounts."""
    raise NotImplementedError
