# Tier 1 Exp 1A — Granularity Test
# Hypothesis: component-level composition beats media-id-only encoding
defaults:
  - base/data
  - base/model
  - base/train
  - base/eval

stage: tier1
experiment_id: T1-E1A
condition_encoder_variants:
  - media_id_only
  - multihot_component_presence
promotion_rule:
  primary: rmse_decrease
  secondary: spearman_non_degradation
