# @package _global_
# T1-D — Metadata Bundle Test. Hypothesis H-ENC-04.
#
# Data: S4 Option D — chemistry = experiment_chemistry.parquet; metadata =
#   experiment_metadata.parquet (no genotype / mutantLibrary in locked S4 table).

experiment_id: T1-D_metadata_bundle
stage_or_tier: T1

experiment:
  hypothesis: H-ENC-04
  arms:
    - feature_bundle: chemistry_only
    - feature_bundle: chemistry_plus_metadata
  seeds: [0, 1, 2]
  promotion_rule:
    additive_baseline_gate: required
    primary: [rmse_decrease, mae_decrease]
