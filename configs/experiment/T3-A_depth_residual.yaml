# @package _global_
# T3-A — Depth/Residual Test. Hypothesis H-CAP-01.
# T3 may not introduce new fusion topologies (locked in T2).

experiment_id: T3-A_depth_residual
stage_or_tier: T3

experiment:
  hypothesis: H-CAP-01
  arms:
    - architecture: residual_mlp_1
    - architecture: residual_mlp_2
    - architecture: residual_mlp_4
  seeds: [0, 1, 2]
  promotion_rule:
    additive_baseline_gate: required
    primary: [rmse_decrease, mae_decrease]
    seed_variance_tolerance: 0.05
