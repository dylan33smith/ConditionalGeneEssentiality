# @package _global_
# T3-D — FiLM at Winning Depth. Hypothesis H-CAP-03.
#
# Re-tests FiLM gating at T3-A's winning depth. Motivated by T2-C's
# near-threshold FiLM signal (MAE gap 80% of bar at 1 layer).
# Depends on T3-A results.

experiment_id: T3-D_film_at_depth
stage_or_tier: T3

experiment:
  hypothesis: H-CAP-03
  arms:
    - fusion: concat
    - fusion: film_gated
  seeds: [0, 1, 2]
  promotion_rule:
    additive_baseline_gate: required
    primary: [rmse_decrease, mae_decrease]
