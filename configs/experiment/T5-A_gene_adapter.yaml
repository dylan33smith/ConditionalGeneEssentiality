# @package _global_
# T5-A — Learnable Gene-Side Adapter. Hypothesis H-EMB-02.
#
# Tests whether a learnable MLP between the frozen gene embedding and the
# concat point unlocks additional signal.

experiment_id: T5-A_gene_adapter
stage_or_tier: T5

experiment:
  hypothesis: H-EMB-02
  arms:
    - adapter: none
    - adapter: 256_hidden
    - adapter: 512_hidden
    - adapter: 1024_hidden_512_out
  seeds: [0, 1, 2]
  promotion_rule:
    primary: [rmse_decrease, mae_decrease]
    no_regression_vs_t3: required
