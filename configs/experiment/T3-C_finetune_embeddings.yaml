# @package _global_
# T3-C (CONDITIONAL) — Embedding Fine-Tune Test. Hypothesis H-EMB-01.
# Only run if T3-A/B plateau against null delta.

experiment_id: T3-C_finetune_embeddings
stage_or_tier: T3

experiment:
  hypothesis: H-EMB-01
  conditional_trigger: t3a_or_t3b_null_delta_plateau
  arms:
    - embeddings: frozen
    - embeddings: finetune_top_2
    - embeddings: finetune_top_4
  seeds: [0, 1, 2]
  promotion_rule:
    additive_baseline_gate: required
    primary: [rmse_decrease, mae_decrease]
